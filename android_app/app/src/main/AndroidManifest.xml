<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.oligark.flashapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".FlashApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.MainActivity">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity
            android:name=".view.LoginActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.UserSelectionActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".view.CustomerMainActivity"
            android:label="@string/title_activity_customer_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".view.SitterMainActivity"
            android:label="@string/title_activity_sitter_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".mascotas" />
        <activity android:name=".add_mascota"
            android:label="Registrar Mascota"></activity>
    </application>

</manifest>